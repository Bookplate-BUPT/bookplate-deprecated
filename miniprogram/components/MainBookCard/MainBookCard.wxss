/* components/mainBook/mainBook.wxss */
.shadow {
  box-shadow: 0px 5px 10px 5px rgba(0, 0, 0, 0.05);
  background-color: #fafafa;
  /* 解决van-swipe-cell渲染层报错 */
  touch-action: pan-y;
}

.tagMargin {
  margin-left: 6px;
}