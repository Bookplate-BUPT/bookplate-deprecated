/* pages/main/main.wxss */
.tagMargin-seek {
  margin-top: 8px;
}

.swiperCellButton {
  --button-default-height: 100%;
  width: auto;
}

.shadow {
  box-shadow: 0px 5px 10px 5px rgba(0, 0, 0, 0.05);
  background-color: #fafafa;
  /* 解决van-swipe-cell渲染层报错 */
  touch-action: pan-y;
}

.styler {
  --notice-bar-font-size: small
}